# SMTP Email Configuration
# Copy this file to .env and update with your actual credentials

# Gmail SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Alternative SMTP Providers:

# Outlook/Hotmail
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# Yahoo Mail
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_USER=your-email@yahoo.com
# SMTP_PASS=your-app-password

# Custom SMTP (like cPanel hosting)
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587
# SMTP_USER=your-email@yourdomain.com
# SMTP_PASS=your-password

# Instructions:
# 1. Copy this file to .env
# 2. Update the values with your actual email credentials
# 3. For Gmail: Enable 2FA and generate App Password at https://myaccount.google.com/apppasswords
# 4. Restart the server after updating .env file
